# Gmail Web Client

Gmail の送受信ができるウェブアプリケーション

## セットアップ

1. `npm install` でパッケージをインストール
2. `.env.example` を `.env` にリネームして、Google API の認証情報を入力
3. `npm start` でサーバーを起動
4. `http://localhost:3000` にアクセス

## 機能

- Gmail のログイン
- 受信メール一覧表示
- メール送信
